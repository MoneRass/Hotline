#include<SFML/Graphics.hpp>
#include<iostream>
#include<stdbool.h>
#include"Bullet.h"

/*class player
{


public:
	
	void playerLook(sf::RenderWindow &window);
	void playerLook2();
	void drawTo(sf::RenderWindow &window);
	player(sf::Vector2f size);
	void setPos(sf::Vector2f newPos);
	void playerUpdate();
	void playerShoot();

	
	
private:
	
	

	int health;
	int ammo;
	int armor;

	bool dead;
	bool shoot;

	sf::Event event;
	
	
	sf::RectangleShape playerBody;
	
	sf::Vector2f playerCenter;
	sf::Vector2f mousePos;
	sf::Vector2f aimDir;
	sf::Vector2f aimDirNorm;
};*/
